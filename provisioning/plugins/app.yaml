apiVersion: 1

apps:
  - type: 'durby-durbysceneabc-app'
    org_id: 1
    org_name: 'durby'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
      isApiKeySet: true
    secureJsonData:
      apiKey: secret-key
